<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Applicacion para admin</a>
        </div>
      </div>
    </nav>

    <section id="main"
		<div class="panel panel-primary">
			<div class="panel-heading">
				<h3 class="panel-title">
Listado de residencias</h3>
			</div>
			<div class="panel-body">
				<table class="table table-striped table-hover">
				<tr>
					<th>Nombre</th>
					<th>Ciudad</th>
          <th>Direccion</th>
          <th>Cantidad de habitaciones</th>
				</tr>
				<% @homes.each do |home| %>
					<tr>
						<td><%= home.nombre %></td>
            <td> <%= home.ciudad %>
            </td>
            <td>
						<%= home.direccion %>
						</td>
						<td><%= home.canthabitacion %></td>
						<td><%= link_to "Editar",edit_home_path(home),class: "btn btn-primary"%>
						<%= link_to "Borrar",home,method: :delete,data:{confirm: "Estas seguro?"},class: "btn btn-primary"%>
						</td>
					</tr>
				<%end%>
				</table>
			</div>
		</div>
    </section>
    <%= link_to "Crear nueva residencia",new_home_path,class: "btn btn-primary"%>
